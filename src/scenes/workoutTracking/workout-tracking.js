import React from 'react';
import {SafeAreaView, Text} from 'react-native';

const WorkoutTrackingScreen = ({navigation}) => (
    <SafeAreaView>
        <Text>Screen: Workout tracking</Text>
    </SafeAreaView>
);

export default WorkoutTrackingScreen;